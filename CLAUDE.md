# Claude Code Instructions

## Git & GitHub

After making any changes to local files, always commit and push them to GitHub:

1. Stage the specific modified files (never use `git add -A` or `git add .`)
2. Commit with a concise message describing the change
3. Push to `origin main`

All changes must be reflected in the remote GitHub repository before the task is considered complete.
